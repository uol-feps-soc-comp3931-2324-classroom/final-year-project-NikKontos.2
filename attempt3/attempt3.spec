# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['attempt3.py'],
    pathex=[],
    binaries=[('/usr/local/lib/libCoinUtils.3.dylib', '.')],
    datas=[('large_invigilators.xlsx', '.'), ('large_exam_venues.xlsx', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='attempt3',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
